<?php

return [
    /*
     * Set to false to prevent the plugin from injecting popup markup
     * into the theme footer automatically.
     */
    'inject_in_theme' => env('POPUP_ADS_INJECT_IN_THEME', true),
];
